<%= form_for(@assets_rating) do |f| %>
  <% if @assets_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assets_rating.errors.count, "error") %> prohibited this assets_rating from being saved:</h2>

      <ul>
      <% @assets_rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asset_id %><br>
    <%= f.number_field :asset_id %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
