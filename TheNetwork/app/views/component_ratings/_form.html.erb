<%= form_for(@component_rating) do |f| %>
  <% if @component_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@component_rating.errors.count, "error") %> prohibited this component_rating from being saved:</h2>

      <ul>
      <% @component_rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :component_id %><br>
    <%= f.number_field :component_id %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
