<%= form_for(@resource_tag) do |f| %>
  <% if @resource_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resource_tag.errors.count, "error") %> prohibited this resource_tag from being saved:</h2>

      <ul>
      <% @resource_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resource_id %><br>
    <%= f.number_field :resource_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
