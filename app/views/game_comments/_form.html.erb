<%= form_for(@game_comment) do |f| %>
  <% if @game_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_comment.errors.count, "error") %> prohibited this game_comment from being saved:</h2>

      <ul>
      <% @game_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.number_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
