<%= form_for(@resource_rating) do |f| %>
  <% if @resource_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resource_rating.errors.count, "error") %> prohibited this resource_rating from being saved:</h2>

      <ul>
      <% @resource_rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resource_id %><br>
    <%= f.number_field :resource_id %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
