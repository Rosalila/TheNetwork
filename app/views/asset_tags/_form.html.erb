<%= form_for(@asset_tag) do |f| %>
  <% if @asset_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asset_tag.errors.count, "error") %> prohibited this asset_tag from being saved:</h2>

      <ul>
      <% @asset_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asset_id %><br>
    <%= f.number_field :asset_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
