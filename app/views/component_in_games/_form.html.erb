<%= form_for(@component_in_game) do |f| %>
  <% if @component_in_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@component_in_game.errors.count, "error") %> prohibited this component_in_game from being saved:</h2>

      <ul>
      <% @component_in_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :component_id %><br>
    <%= f.number_field :component_id %>
  </div>
  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.number_field :game_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
