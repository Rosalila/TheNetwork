<%= form_for(@engine_comment) do |f| %>
  <% if @engine_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@engine_comment.errors.count, "error") %> prohibited this engine_comment from being saved:</h2>

      <ul>
      <% @engine_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :engine_id %><br>
    <%= f.number_field :engine_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
