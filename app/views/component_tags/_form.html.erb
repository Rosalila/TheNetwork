<%= form_for(@component_tag) do |f| %>
  <% if @component_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@component_tag.errors.count, "error") %> prohibited this component_tag from being saved:</h2>

      <ul>
      <% @component_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :component_id %><br>
    <%= f.number_field :component_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
