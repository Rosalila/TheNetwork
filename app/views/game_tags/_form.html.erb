<%= form_for(@game_tag) do |f| %>
  <% if @game_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_tag.errors.count, "error") %> prohibited this game_tag from being saved:</h2>

      <ul>
      <% @game_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.number_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
