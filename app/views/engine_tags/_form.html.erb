<%= form_for(@engine_tag) do |f| %>
  <% if @engine_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@engine_tag.errors.count, "error") %> prohibited this engine_tag from being saved:</h2>

      <ul>
      <% @engine_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :engine_id %><br>
    <%= f.number_field :engine_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
